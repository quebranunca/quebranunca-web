import { ArenaSection } from './ArenaSection';

export function ArenaPlaceholderSection({ titulo, descricao, placeholder }) {
  return (
    <ArenaSection
      titulo={titulo}
      descricao={descricao}
      placeholder={placeholder}
    />
  );
}
